# -*- coding: utf-8 -*-
"""
config.py — ค่าตั้งต่าง ๆ ของระบบ (แก้ที่นี่ที่เดียว)
โปรเจกต์: ระบบติดตามค่าฝุ่น PM2.5 ด้วย Raspberry Pi 5

เซนเซอร์ที่ใช้:
  - PMS3003  : วัดค่าฝุ่น PM2.5 จริง (ต่อผ่าน UART)
  - DHT22    : วัดอุณหภูมิ / ความชื้น (ต่อผ่าน GPIO)
  - Webcam C270 : ถ่ายภาพท้องฟ้า (USB)
"""

# ---- เซนเซอร์ฝุ่น PMS3003 (UART) ----
# บน Raspberry Pi 5 พอร์ต UART คือ /dev/serial0
PMS3003_PORT = "/dev/serial0"
PMS3003_BAUD = 9600

# ---- เซนเซอร์อุณหภูมิ/ความชื้น DHT22 ----
# ต่อสาย DATA เข้าขา GPIO4 (pin 7) -> เขียนเป็น "D4"
DHT22_PIN = "D4"

# ---- กล้อง webcam ----
CAMERA_INDEX = 0        # C270 มักเป็นกล้องตัวแรก = 0
CAMERA_WIDTH = 1280
CAMERA_HEIGHT = 720

# ---- รอบการทำงาน ----
READ_INTERVAL = 60      # อ่านค่าทุกกี่วินาที (ตั้ง 60 = ทุก 1 นาที)

# ---- โหมดจำลอง ----
# True  = สร้างข้อมูลปลอมไว้ทดสอบ (ใช้ตอนยังไม่ต่อเซนเซอร์ / รันบนคอม)
# False = อ่านจากเซนเซอร์จริงบน Pi 5
# ตั้งเป็น True ไว้ก่อนตอนทดสอบ พอต่ออุปกรณ์ครบแล้วค่อยเปลี่ยนเป็น False
USE_MOCK = True

# ---- ที่เก็บข้อมูล ----
DB_FILE = "pm25.db"     # ไฟล์ฐานข้อมูล SQLite (สร้างอัตโนมัติ)

# ---- เว็บเซิร์ฟเวอร์ (Dashboard) ----
WEB_HOST = "0.0.0.0"    # 0.0.0.0 = เปิดให้เครื่องอื่นในวง Wi-Fi เดียวกันเข้าดูได้
WEB_PORT = 8000
